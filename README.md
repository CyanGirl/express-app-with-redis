## Express JS App with Redis

A simple app made with Express JS and Redis integration for quickstart.

## Prerequisites:

- Node
- Redis

## Start Redis Server:

- Run the server : `redis-server`

## To run:

- Navigate to this directory
- Install dependencies with: `npm install`
- Run: `node app.js`
